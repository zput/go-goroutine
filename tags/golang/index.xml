<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>golang on go调度源码分析</title><link>https://zput.github.io/go-goroutine/tags/golang/</link><description>Recent content in golang on go调度源码分析</description><generator>Hugo -- gohugo.io</generator><language>zh</language><lastBuildDate>Mon, 04 May 2020 12:35:00 +0000</lastBuildDate><atom:link href="https://zput.github.io/go-goroutine/tags/golang/index.xml" rel="self" type="application/rss+xml"/><item><title>Go 汇编</title><link>https://zput.github.io/go-goroutine/docs/runtime/1.assemble/</link><pubDate>Thu, 28 Feb 2019 15:03:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/1.assemble/</guid><description>1.伪寄存器 # 伪寄存器常用的一般是下面的四个:
FP: Frame pointer: arguments and locals. PC: Program counter: jumps and branches. SB: Static base pointer: global symbols. SP: Stack pointer: top of stack. 下面我们来翻译一下 官网1的对他们的解释，然后做一个总结,方便理解。
1.1.FP # FP伪寄存器是一个用于引用函数参数的虚拟帧指针。编译器维护一个虚拟帧指针，并将堆栈上的参数引用为该伪寄存器的偏移量。因此0(FP)是函数的第一个参数，8(FP)是第二个参数(在64位机器上)，以此类推。但是，当以这种方式引用一个函数参数时，有必要将名称放在开头，如first_arg+0(FP)和second_arg+8(FP)。(偏移量的含义(从帧指针出发的偏移量)与它在SB中的使用不同，在SB中，它是从符号出发的偏移量。) 汇编器强制执行这个约定，拒绝普通的0(FP)和8(FP)。实际的名称在语义上是不相关的，但应该用来记录参数的名称。值得强调的是，FP始终是一个伪寄存器，而不是硬件寄存器，即使在具有硬件帧指针的架构上也是如此。
The FP pseudo-register is a virtual frame pointer used to refer to function arguments. The compilers maintain a virtual frame pointer and refer to the arguments on the stack as offsets from that pseudo-register. Thus 0(FP) is the first argument to the function, 8(FP) is the second (on a 64-bit machine), and so on.</description></item><item><title>初始化</title><link>https://zput.github.io/go-goroutine/docs/runtime/schedule/1_init_before_enter_main_function/</link><pubDate>Mon, 20 Jan 2020 10:03:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/schedule/1_init_before_enter_main_function/</guid><description>golang程序不是直接执行main函数里的内容，在进入之前会进行一些初始化，本章来探寻初始化逻辑。
1.前言 # 我们首先来gdb调试一下这个程序
// main.go package main import &amp;#34;fmt&amp;#34; // the function&amp;#39;s body is empty func add(x, y int64) int64 func main() { gg:=add(2, 3) fmt.Println(gg) } // add_amd.s TEXT ·add(SB),$0-24 MOVQ x+0(FP), BX MOVQ y+8(FP), BP ADDQ BP, BX MOVQ BX, ret+16(FP) RET 编译一下源代码: go build -gcflags &amp;quot;-N -l&amp;quot; -o test .
2.程序加载到内存入口 # Expand ↕ (gdb) info files
Symbols from &amp;#34;/tmp/kubernets/test&amp;#34;. Local exec file: `/tmp/kubernets/test&amp;#39;, file type elf64-x86-64.</description></item><item><title>goroutine退出过程</title><link>https://zput.github.io/go-goroutine/docs/runtime/schedule/2_exit_goroutine/</link><pubDate>Mon, 10 Feb 2020 10:09:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/schedule/2_exit_goroutine/</guid><description>goroutine退出，即执行完调用代码(callee)后，返回到调用者代码(caller)中去，前面一节我们看到，编译器自己把goexit()的地址设置为了调用者(caller)的pc保存到栈上方,所以退出后，会执行goexit()函数。
但main goroutine比较特殊，这个groutine运行的代码 main函数在内部直接调用了操作系统的exit接口,进程主动终止自身的执行.
1.main goroutine的退出 # 在上节中我们看到程序执行到了 mian函数
func main() { g := getg() //... //main包 init函数，递归的调用import包中定义的init函数 fn := main_init fn() //... //调用main.main函数(用户定义的main函数):进行间接调用是因为链接器在放置运行时不知道主包的地址 fn = main_main fn() //... //系统API:exit函数,退出进程 exit(0) for { var x *int32 *x = 0 // 无效指针代码，会导致程序退出 } } exit(0)函数和最底部的for循环会让程序不可能回到调用层(caller)
2.普通goroutine的退出 # 我们首先来gdb调试一下这个程序
// main.go package main import &amp;#34;time&amp;#34; func add(x, y int64) int64 func main() { go add(2, 3) time.Sleep(time.Minute) } // add_amd.s TEXT ·add(SB),$0-24 MOVQ x+0(FP), BX MOVQ y+8(FP), BP ADDQ BP, BX MOVQ BX, ret+16(FP) RET 编译一下源代码: go build -gcflags &amp;quot;-N -l&amp;quot; -o test .</description></item><item><title>goroutine被挑选执行的三种策略</title><link>https://zput.github.io/go-goroutine/docs/runtime/schedule/3_scheduling_selection_strategy/</link><pubDate>Mon, 02 Mar 2020 13:03:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/schedule/3_scheduling_selection_strategy/</guid><description>1.调度 # 一轮调度：找到可运行的goroutine并执行它。即由g0-&amp;gt;g, 当g0在m上运行时,找到一个新的goroutine，然后m运行新的goroutine的过程。
findRunnable：找到可运行的goroutine。 execute函数：执行找到的goroutine。 // One round of scheduler: find a runnable goroutine and execute it. // Never returns. func schedule() { _g_ := getg() //... top: //... var gp *g var inheritTime bool //... if gp == nil { if _g_.m.p.ptr().schedtick%61 == 0 &amp;amp;&amp;amp; sched.runqsize &amp;gt; 0 { //schedtick就是调度次数,如果能被61整除且全局的Goroutine队列不为空就尝试获取 lock(&amp;amp;sched.lock) gp = globrunqget(_g_.m.p.ptr(), 1) //全局运行队列中获取goroutine unlock(&amp;amp;sched.lock) } } if gp == nil { gp, inheritTime = runqget(_g_.</description></item><item><title>goroutine主动让出执行</title><link>https://zput.github.io/go-goroutine/docs/runtime/schedule/5_goroutine_give_up_voluntarily/</link><pubDate>Thu, 02 Apr 2020 11:03:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/schedule/5_goroutine_give_up_voluntarily/</guid><description>即由g-&amp;gt;g0, 当g在m上运行时,主动让出m，然后m重新运行g0的过程。
1.主动让出之业务手动调用 # goroutine主动调度就是调用runtime.Gosched()函数, 此函数会主动放弃与M的关联,并把自身(G)放入全局空闲G队列.当前的goroutine并不会退出,它进入全局G队列后可能会再次被调度运行.
//src/runtime/proc.go:267 func Gosched() { checkTimeouts() mcall(gosched_m) } 在go1.14前,编译器不会插入抢占点,用户代码可能会陷入CPU计算循环(即使代码中有函数调用),它可能会一直霸占CPU,这时候可能需要runtime.Gosched, 在go1.14,编译器会在函数前加入自动抢占点.所以对于go1.14+，无需手动调用它。
源码探寻 # main.go
package main import ( &amp;#34;runtime&amp;#34; &amp;#34;sync/atomic&amp;#34; ) var existFlag int32 = 2 // the function&amp;#39;s body is empty func addAssemble(x, y int64) int64 func add(a int64){ addAssemble(a, a) atomic.AddInt32(&amp;amp;existFlag, -1) } func main() { runtime.GOMAXPROCS(1) go add(1) go add(2) for { if atomic.LoadInt32(&amp;amp;existFlag) &amp;lt;=0{ break } runtime.Gosched() } } add_amd.s
TEXT ·addAssemble(SB),$0-24 MOVQ x+0(FP), BX MOVQ y+8(FP), BP ADDQ BP, BX MOVQ BX, ret+16(FP) RET 编译程序</description></item><item><title>goroutine被剥夺执行</title><link>https://zput.github.io/go-goroutine/docs/runtime/schedule/6_deprivation_goroutine_running/</link><pubDate>Mon, 04 May 2020 12:35:00 +0000</pubDate><guid>https://zput.github.io/go-goroutine/docs/runtime/schedule/6_deprivation_goroutine_running/</guid><description>1.系统监控 # 这个是main goroutine执行的主函数，此函数之后会调用用户定义main函数,我们在初始化一章已经提过main goroutine执行的主函数
它的里面可以看到调用了newm函数生成新M,且新M将执行sysmon函数
func main() { //... if GOARCH != &amp;#34;wasm&amp;#34; { // no threads on wasm yet, so no sysmon systemstack(func() { newm(sysmon, nil) }) } //... } sysmon # 可以看到sysmon里面是一个for循环,它会一直运行,因为这个M没有关联P，所以不允许写屏障 逻辑比较简单，延时睡眠，调用retake
// Always runs without a P, so write barriers are not allowed. // //go:nowritebarrierrec func sysmon() { lock(&amp;amp;sched.lock) sched.nmsys++ //增加记录系统线程的值的个数 checkdead() unlock(&amp;amp;sched.lock) lasttrace := int64(0) idle := 0 // how many cycles in succession we had not wokeup somebody delay := uint32(0) for { if idle == 0 { // start with 20us sleep.</description></item></channel></rss>